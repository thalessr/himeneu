<% if can? :read, @customer %>
    <div class="panel">
    	<div class="col-md-12">
            <div class="col-md-8">
                <h5><%= @customer.full_name %></h5>
                <% if @customer.address %>
                    <address>
    				  <%= @customer.city %><br>
    				  <%= @customer.zipcode %><br>
    				  <abbr title="<%= t(:phone) %>"><%= t(:phone) %></abbr> <%= @customer.phone %>
                       <p><%= t(:email) %> <%= mail_to @customer.email %>.</p>
    				</address>
                    <%= image_tag "http://maps.google.com/maps/api/staticmap?size=450x300&sensor=false&zoom=16&markers=#{@customer.latitude}%2C#{@customer.longitude}" %>
    			<% end %>
                <!-- Indeed it will become a contact form -->

            </div>
             <div class="col-md-4">
                <% if @customer.image_processing %>
                     <%= image_tag('loading.gif', class: "img-responsive") %>
                <% else %>
            		<%= image_tag @customer.image_url(:thumb),  size: "304x236", :class => "img-thumbnail" if @customer.image? %>
                <% end %>
        	</div>
        </div>
        <% if can? :update, @customer %>
        	<%= link_to t('links.edit'), edit_customer_path  %>
        <% end %>
    </div>
<% end %>